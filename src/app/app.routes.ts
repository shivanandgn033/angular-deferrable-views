import { Routes } from '@angular/router';
import { TableexampleComponent } from './example1/tableexample/tableexample.component';

export const routes: Routes = [
    {
        path:'',
        component:TableexampleComponent
    }
];
